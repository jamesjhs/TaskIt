# better-sqlite3-multiple-ciphers uses N-API and works on any Node >=20,
# but its package.json engines field hasn't been updated to include Node 25.x yet.
# ignore-engines suppresses that false-positive EBADENGINE warning.
ignore-engines=true
